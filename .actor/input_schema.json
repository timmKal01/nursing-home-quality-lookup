{
    "title": "Nursing Home Quality Lookup Input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "name": {
            "title": "Facility name",
            "type": "string",
            "editor": "textfield",
            "description": "Full or partial nursing home name to search for. Leave blank to browse by state instead."
        },
        "state": {
            "title": "State",
            "type": "string",
            "editor": "textfield",
            "description": "Two-letter US state code to limit to facilities located there, e.g. \"CA\"."
        },
        "minRating": {
            "title": "Minimum overall star rating",
            "type": "integer",
            "editor": "number",
            "description": "Only return facilities with at least this CMS overall rating (1-5 stars). Leave blank for all rated facilities.",
            "minimum": 1,
            "maximum": 5
        },
        "maxResults": {
            "title": "Max results",
            "type": "integer",
            "editor": "number",
            "description": "Maximum number of facilities to return.",
            "default": 25,
            "minimum": 1,
            "maximum": 100
        }
    },
    "required": []
}
